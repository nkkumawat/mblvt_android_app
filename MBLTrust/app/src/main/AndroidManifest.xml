<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sonu.mbltrust">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mblvt_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FeedBack" />
        <activity
            android:name=".all"
            android:label="@string/title_activity_all"
            android:theme="@style/AppTheme" />
        <activity android:name=".Contact" />
        <activity android:name=".todayThought" />
        <activity android:name=".jsonActiviy" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Gallery1"></activity>
    </application>

</manifest>